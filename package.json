{
  "name": "continuation-layer",
  "version": "0.0.0",
  "private": true,
  "description": "Continuity guard for CLI coding agents. Phase 0 scaffold only.",
  "type": "module",
  "scripts": {
    "check": "node --version",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}

